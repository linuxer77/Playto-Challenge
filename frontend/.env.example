# Frontend API base URL
# For local dev with Vite proxy, leave this empty or remove it.
# Set an absolute URL only when you intentionally need cross-origin calls.
VITE_API_BASE_URL=

# Optional dev proxy target in vite.config.js
VITE_API_TARGET=http://localhost:8000
